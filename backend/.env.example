# Optional: required for private repositories and GitHub API access
GITHUB_TOKEN=

# Optional: required for private GitLab repositories and GitLab API access
GITLAB_TOKEN=

SECRET_KEY=replace-with-a-long-random-secret
FLASK_ENV=development
FLASK_DEBUG=False
DATABASE_PATH=data/app.db
CORS_ORIGINS=http://localhost:5173
